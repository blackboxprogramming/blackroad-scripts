import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Color Playground', description: 'Explore the BlackRoad OS color palette — 30+ brand colors with CSS variables.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
