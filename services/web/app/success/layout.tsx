import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Welcome to BlackRoad OS', description: 'Your subscription is active. Get started with your AI agent deployment.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
