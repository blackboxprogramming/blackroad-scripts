import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Operations Dashboard', description: 'Real-time operations dashboard with deployment tracking and service metrics.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
