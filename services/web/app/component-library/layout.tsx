import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Component Library', description: 'BlackRoad OS UI component library — buttons, cards, inputs, and visual effects.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
